# Map browser (0.9.2)

`WINDOWS-START-HERE.md` を参照し、v15の `Build-Live.cmd` を実行してください。
最初のティアだけ展開し、残りは見出しクリックで開閉します。
個別のマップデータは含まず、サーバーのmaplist.txtから読み込んだ文字列を表示します。
素材には共通960枠がありますが、送信量を抑えるため使用するのは最大26枠です。
24行ずつ表示し、残り2枠を前後のページ移動に使用します。
一覧内の `Next page >>` / `<< Previous page` で全マップを確認できます。
プレイヤーごとに HUD を作成し、閉じると破棄します。既存の素材の再ビルド・再公開は不要です。
